<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Match</title>
    <style>
        /* Global Styles */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        
        /* Container Styles */
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            color: #ff6363; /* Red text color */
            background-color: #ffecf2; /* Light red background */
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
        }
        
        /* Header Styles */
        h1 {
            color: #321E1E; /* Pinkish-red color */
            text-align: center;
            font-weight: bold; /* Add font weight for better readability */
        }
        
        /* Form Styles */
        form {
            margin-top: 20px;
            color: #321E1E;
        }

        label {
            font-weight: bold;
            margin-bottom: 5px;
            display: block;
        }

        .team-container {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .team-container label {
            display: block;
            margin-bottom: 5px;
        }

        .team-container input[type="checkbox"] {
            margin-right: 10px;
            transform: scale(1.5); /* Increase checkbox size */
        }

        input[type="number"],
        select {
            width: calc(50% - 5px);
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }

        input[type="submit"] {
            background-color: #e73a5f; /* Pinkish-red background */
            color: #fff; /* White text color */
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            width: 100%;
        }

        input[type="submit"]:hover {
            background-color: #c1274d; /* Darker pinkish-red background on hover */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Create Match</h1>
        <form action="/creatematch" method="post">
            <label for="overs">Number of Overs:</label>
            <input type="number" id="overs" name="overs" min="1" required>

            <div class="team-container">
                <div>
                    <label for="team1">Team 1:</label>
                    <% for (let i = 0; i < players.length; i++) { %>
                        <label>
                            <input type="checkbox" name="team1[]" value="<%= players[i]._id %>">
                            <%= players[i].name %>
                        </label>
                    <% } %>
                </div>
                <div>
                    <label for="team2">Team 2:</label>
                    <% for (let i = 0; i < players.length; i++) { %>
                        <label>
                            <input type="checkbox" name="team2[]" value="<%= players[i]._id %>">
                            <%= players[i].name %>
                        </label>
                    <% } %>
                </div>
            </div>

            <label for="firstBatting">First Batting:</label>
            <select id="firstBatting" name="firstBatting" required>
                <option value="team1">Team 1</option>
                <option value="team2">Team 2</option>
            </select>

            <input type="submit" value="Create Match">
        </form>
    </div>
</body>
</html>
